# Coordinate GitHub Repositories v1.8.0

Version `1.8.0` presents the confirmed founding use case and the skill's value-preservation goal directly in the public documentation while preserving the installable runtime unchanged.

## Product Documentation

- Lead the README with the repository-sprawl problem, portfolio-level judgment, and value preservation.
- Record that the maintainer coordinates 340 projects across a personal account and two organizations.
- Explain how a structured inventory, a cross-repository coordination tracker, and a partial repository manager each solved part of the problem before this skill distilled their useful parts.
- State that the skill runs daily on the founding portfolio while distinguishing that validation from external adoption evidence.
- Name the ten runtime problem classes so readers can recognize whether the skill fits their situation.

## Evidence and Demonstration

- Route readers to the approved founding case for a real result, its participant relationship, privacy boundary, and limitations.
- Defer a public conversational excerpt until a real session is sanitized and the exact text is approved for publication.
- Record the three-implementation lineage in architecture and provenance without publishing private repository names, paths, records, identities, or raw intake.
- Align roadmap and quickstart language with the distinction between founding validation and external adoption evidence.

## Compatibility

This minor release changes public product documentation and synchronized release metadata. It leaves `skills/coordinate-github-repositories/SKILL.md`, every runtime reference, activation and output behavior, fixtures, package formats, installation paths, and the conversation-only fallback unchanged.

## Verification

Run the final release gate with the intended tag:

```bash
npm run release:preflight -- v1.8.0
```

Inspect all three ZIP files under `dist/assets/`. Confirm that runtime content remains byte-identical across packages and that documentation, fixtures, temporary material, private intake, credentials, and local paths remain excluded.

Release tag: `v1.8.0`.
