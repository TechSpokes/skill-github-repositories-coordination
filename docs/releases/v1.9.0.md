# Coordinate GitHub Repositories v1.9.0

Version `1.9.0` replaces the abstract founding demonstration with a concrete, auditable account of the skill reusing release knowledge across the maintainer's 340-project portfolio.

## Real Use Case

- Lead the README demonstration with the maintainer's request to apply relevant release practices from three repositories they already managed across two organizations.
- Show how the portfolio index located a package template, a production application cutover, and a deployed service release model.
- Explain which controls fit this installable skill and which application and deployment controls did not.
- Link the result to pull request #25 and the release scripts and workflows that still implement the combined design.
- Follow the process through the unpublished `v1.3.1` candidate and the `v1.3.2` correction that normalized cross-host package content and added guarded abandonment.

## Public Language

- Replace landing-page labels such as approved result, participant relationship, founding validation, and external adoption boundary with the real request, work, and outcome.
- State plainly that this is the builder and maintainer's own daily use and that the proof is preserved in repository history and operating automation.
- Keep source repository identities, local paths, credentials, raw portfolio records, and unrelated application details private.

## Compatibility

This minor release changes public product documentation and synchronized release metadata. It leaves `skills/coordinate-github-repositories/SKILL.md`, every runtime reference, activation and output behavior, fixtures, permissions, package formats, installation paths, and the conversation-only fallback unchanged.

## Verification

Run the final release gate with the intended tag:

```bash
npm run release:preflight -- v1.9.0
```

Inspect all three ZIP files under `dist/assets/`. Confirm that runtime content remains byte-identical across packages and that documentation, fixtures, temporary material, private intake, credentials, local paths, and private repository identities remain excluded.

Release tag: `v1.9.0`.
